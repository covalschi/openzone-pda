// Розділи ПРИСТРОЮ: те, що КПК тримає в собі сам.
//
// Різниця з носієм не в реалізації, а в призначенні, і плутати їх не можна:
//
//   ПРИСТРІЙ  те, чим користуються. Пін, нотатки, маршрут, мітки й тепер
//             будь-який розділ чужого модуля живуть тут. Витягнута дискета
//             не мусить забирати з собою те, на що ти сам налаштував прилад.
//   НОСІЙ     знімне, чим ПЕРЕНОСЯТЬ. Його можна загубити, зняти з тіла,
//             віддати товаришу -- і саме тому він не місце для робочих
//             даних, а місце для копії.
//
// Звідси й різні стелі, і це навмисно:
//
//   на пристрої  СВОЯ НА КОЖЕН РОЗДІЛ -- скільки нотаток, скільки міток,
//                скільки частот. Прилад знає, з чим працює.
//   на носії     ОДНА ЗАГАЛЬНА В ЗАПИСАХ -- він не знає, що везе, і не
//                мусить знати наперед про кожен майбутній рід.
//
// Форма спільна з носієм навмисно: перенести розділ туди й назад -- це має
// бути копіювання, а не переклад.

class OZ_PdaSection
{
    string Kind    = "";
    string Payload = "";
    // Скільки записів коштує розділ. Рахує ТОЙ, ХТО ПИШЕ: лише він знає, що
    // всередині його JSON.
    int    Records = 0;
}
